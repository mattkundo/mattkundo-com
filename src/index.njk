---
layout: base.njk
title: "Matt Kundo: Marketing operations and GTM engineering"
---
<header class="topbar">
  <span>{{ site.name }}</span>
  <span>{{ site.location }}</span>
</header>

<main class="wrap">

  <div class="idblock">
    <img class="portrait" src="/assets/img/matt-kundo-portrait.jpg"
         alt="Matt Kundo" width="104" height="104">
    <div>
      <h1>{{ site.name }}</h1>
      <p class="sub">{{ site.roleLine }}</p>
      <p class="hook">Marketer who builds the internal tools.</p>
    </div>
  </div>

  <p class="pitch">Ten years across paid media, SEO, analytics and CRM operations. The last
    two I spent building the GTM infrastructure itself, with AI as the leverage. I am
    winding down my agency for an in-house role.</p>

  <div class="metrics">
    <div>
      <div class="v">{{ metrics.metrics.merged_prs.value }}</div>
      <div class="l">pull requests merged in {{ metrics.metrics.elapsed_days.value }} days,
        one person and the platform</div>
    </div>
    <div>
      <div class="v">{{ metrics.metrics.tools.value }}</div>
      <div class="l">agent-callable tools I built and operate</div>
    </div>
    <div>
      <div class="v">101%</div>
      <div class="l">measured ROI on a $76,279 channel mix</div>
    </div>
    <div>
      <div class="v">{{ metrics.metrics.tests_passed.value | commas }}</div>
      <div class="l">tests passing, which is how the speed stays honest</div>
    </div>
  </div>

  <p class="ctas">
    <a class="btn" href="/assets/resume/matt-kundo-resume.pdf">Download resume (PDF)</a>
    <a class="btn2" href="mailto:{{ site.email }}">Email me</a>
  </p>
  <p class="quiet">
    <a href="{{ site.linkedin }}" rel="me">linkedin.com/in/mattkundo</a>
    <a href="{{ site.github }}" rel="me">github.com/mattkundo</a>
    <a href="{{ site.product }}">texascommercialplans.com</a>
  </p>

  <h2 class="sh">The platform</h2>
  <p class="pitch">A production autonomous agent system in Python that I built to work
    alongside me across my client book. Postgres with pgvector semantic memory, deterministic
    compliance gates, database-backed kill switches, and a completion gate that requires
    evidence before a task can be called done.</p>
  <p class="limits">It does not run the operation. It plays critical support, and it extends
    what I can deliver. Client email is draft-only except the weekly performance report, which
    auto-sends only when I arm it. Monthly reports need a manual confirm before release. Client
    ad budget, bid and copy mutations raise NotImplementedError; one account has a narrow,
    kill-switched autopilot that can only pause spend, never raise it. Stating the limits is
    what makes the rest credible.</p>
  <p class="row"><span class="k">scale</span><span>{{ metrics.metrics.python_lines.value | commas }}
    lines of Python, {{ metrics.metrics.scheduled_jobs.value }} scheduled jobs,
    {{ metrics.metrics.agent_roles.value }} agent roles</span></p>
  <p class="row"><span class="k">measured</span><span>as of {{ metrics.snapshot }}, derived
    from the repository rather than typed by hand</span></p>

  <h2 class="sh">Results</h2>
  <ul>
    <li><b>101% ROI</b> on a $76,279 channel mix for a retail electricity provider. $153,610
      revenue, 594 enrollments at $128.45 each, measured through an attribution model I built.</li>
    <li><b>85% CPA reduction</b> with 65% growth on a healthcare e-commerce account.</li>
    <li><b>42.6% conversion rate</b> on a rebuilt landing page, 1,662 conversions.</li>
    <li><b>+113% Google Business Profile interactions</b> for a multi-location energy client.</li>
  </ul>
  <p class="quiet">Clients anonymized by agreement. Every figure traces to a named system of record.</p>

  <h2 class="sh">The arc</h2>
  <p class="row"><span class="k">2015 to 2018</span><span>Paid media and SEO, agency and in-house</span></p>
  <p class="row"><span class="k">2018 to now</span><span>Founded MKDM. 30+ clients, $142,841
    managed ad spend in the trailing 90 days as of 2026-08-10</span></p>
  <p class="row"><span class="k">2026 to now</span><span>Built the agent platform. Shipped
    texascommercialplans.com solo: scraper, database, site, content engine</span></p>
  <p class="row"><span class="k">next</span><span>In-house, where I can build one system
    properly instead of five in parallel</span></p>

  <footer class="foot">
    <span>{{ site.location }} · open to marketing operations and GTM engineering roles ·
      <a href="{{ site.sourceRepo }}">this page is open source</a></span>
    <span class="ctas">
      <a class="btn" href="/assets/resume/matt-kundo-resume.pdf">Download resume (PDF)</a>
      <a class="btn2" href="mailto:{{ site.email }}">Email me</a>
    </span>
  </footer>

</main>
